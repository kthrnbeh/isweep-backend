# .env.example
# Copy this to .env and fill in your values

# Database path (SQLite file location)
ISWEEP_DB_PATH=isweep.db

# Debug mode (enables SQL query logging)
ISWEEP_DEBUG=false

# Server configuration (used by __main__.py)
# HOST=127.0.0.1
# PORT=8000
